---
sources:
    hub:
        # List of all available images with associated sha:
        # skopeo --override-os linux inspect  docker://docker.io/bitnami/keycloak
        # Note: use the global index image digest to make this image platform agnostic
        # skopeo --override-os linux inspect docker://docker.io/bitnami/keycloak:<tag> --raw | skopeo manifest-digest /dev/stdin
        image:
            repository: docker.io/bitnami/keycloak
            tag: 26.2.5-debian-12-r2
            digest: sha256:e623429e6967f2080122a1d491394d20adf7a913bf12aea45c7b48617946f394

    prem:
        # List of all available images with associated sha:
        # skopeo --override-os linux inspect docker://registry.camunda.cloud/dockerhub-infraex/bitnamiprem/keycloak
        # Note: use the global index image digest to make this image platform agnostic
        # skopeo --override-os linux inspect docker://registry.camunda.cloud/dockerhub-infraex/bitnamiprem/keycloak:<tag> --raw | skopeo manifest-digest /dev/stdin
        image:
            repository: registry.camunda.cloud/vendor-ee/keycloak
            tag: 26.3.3-debian-12-r2@sha256:9a0a5b5f28e2bc3c54ff2817027b0f3cf83ddbed9ddd527cfb31babfb81abcfd
